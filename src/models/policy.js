// @flow

/**
 * Policy model.
 */
export type Policy = {|
    id: string,
    amountInsured: string,
    email: string,
    inceptionDate: string,
    installmentPayment: string,
    clientId: string
|};
